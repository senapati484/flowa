def process(data):
    return data |> map(lambda x: x * 2) |> list() |> sum()

print("Running Flowa Example")
data = [1, 2, 3, 4, 5]
result = process(data)
print("Result: " + str(result))

async def async_main():
    print("Async start")
    return 100

spawn async_main()
