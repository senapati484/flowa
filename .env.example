# Flowa Environment Configuration

# SMTP Email Settings (for mail module)
# Get your SMTP credentials from your email provider
# For Gmail: Use an App Password, not your regular password
# https://support.google.com/accounts/answer/185833

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Server Configuration
PORT=8080

# JWT Secret (for token signing)
JWT_SECRET=your-super-secret-key-keep-it-safe

# Database (future)
# DATABASE_URL=postgres://localhost/flowa_db
