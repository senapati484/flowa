# Test Auth Module
print("Testing Auth Module...")

# Test password hashing
password = "mySecurePassword123"
hashed = auth.hash_password(password)
print("Hashed password:", hashed)

# Test password verification - correct password
is_valid = auth.verify_password(hashed, password)
print("Password verification (correct):", is_valid)

# Test password verification - wrong password
is_invalid = auth.verify_password(hashed, "wrongPassword")
print("Password verification (wrong):", is_invalid)

print("✅ Auth test completed.")
